{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "today",
            "type": "shell",
            "command": "${workspaceRoot}${/}bin${/}today.sh",
            "runOptions": {
                "runOn": "folderOpen"
            }
        },
        {
            "label": "tomorrow",
            "type": "shell",
            "command": "${workspaceRoot}${/}bin${/}tomorrow.sh",
        }
    ]
}